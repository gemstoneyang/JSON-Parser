{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug (LLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/json_parser",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build (debug)",
        }
    ],
}